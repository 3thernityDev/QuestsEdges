NODE_ENV=production
PORT=3000

# Database
DATABASE_URL={{ database_url }}

# JWT
JWT_SECRET={{ jwt_secret }}

# Azure OAuth
AZURE_CLIENT_ID={{ azure_client_id | default('') }}
AZURE_SECRET={{ azure_secret | default('') }}

# URLs
FRONTEND_URL={{ frontend_url | default('') }}
ALLOWED_ORIGINS={{ allowed_origins | default('') }}
